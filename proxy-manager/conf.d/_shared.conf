# Shared http-level directives, included ahead of the generated host configs.

# Map used to support WebSocket connection upgrades.
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
